id: TKT-002
bundle: "B-011"
type: worker
title: "pipeline/ep_cli.py + pipeline/tests/test_ep_cli.py — Agent CLI v1 with --json"
description: |
  Create the Agent CLI v1 entry point that uses ControlPlaneService (from TKT-001)
  and cli_shared framework for consistent --output json support.

  FILE 1: pipeline/ep_cli.py (NEW — create from scratch)

  Create this file with the following exact structure:

  ```python
  #!/usr/bin/env python3
  """Agent CLI v1 for ExperimentPipeline control plane.

  Provides machine-readable (--output json) and human-readable access to
  experiment status, cluster health, and summary queries.

  Usage:
      python3 pipeline/ep_cli.py status --output json
      python3 pipeline/ep_cli.py experiments --output json
      python3 pipeline/ep_cli.py experiment <name> --output json
      python3 pipeline/ep_cli.py cluster --output json
  """
  from __future__ import annotations

  import argparse
  import sys
  from typing import Sequence

  try:
      from cli_shared import add_common_args, emit_result, setup_logging
      from control_plane import ControlPlaneService
  except ModuleNotFoundError:
      from pipeline.cli_shared import add_common_args, emit_result, setup_logging
      from pipeline.control_plane import ControlPlaneService


  def _build_parser() -> argparse.ArgumentParser:
      parser = argparse.ArgumentParser(
          prog="ep-cli",
          description="ExperimentPipeline Agent CLI v1",
      )
      add_common_args(parser)
      sub = parser.add_subparsers(dest="command")

      sub.add_parser("status", help="Overall status summary")

      exp_list = sub.add_parser("experiments", help="List experiments")
      exp_list.add_argument("--page", type=int, default=1)
      exp_list.add_argument("--per-page", type=int, default=50)

      exp_detail = sub.add_parser("experiment", help="Single experiment detail")
      exp_detail.add_argument("name", help="Experiment name")

      sub.add_parser("cluster", help="Cluster health summary")

      return parser


  def _run(args: argparse.Namespace) -> int:
      setup_logging(args)
      svc = ControlPlaneService()
      command = args.command

      if command == "status":
          data = svc.get_status_summary()
      elif command == "experiments":
          data = svc.list_experiments(
              page=args.page,
              per_page=getattr(args, "per_page", 50),
          )
      elif command == "experiment":
          data = svc.get_experiment(args.name)
          if data is None:
              emit_result(
                  args,
                  {"error": {"message": f"Experiment '{args.name}' not found"}},
                  status="error",
              )
              return 1
      elif command == "cluster":
          data = svc.get_cluster_health()
      else:
          emit_result(
              args,
              {"error": {"message": "No command specified. Use --help."}},
              status="error",
          )
          return 1

      emit_result(args, data, status="ok")
      return 0


  def main(argv: Sequence[str] | None = None) -> int:
      parser = _build_parser()
      args = parser.parse_args(argv)
      return _run(args)


  if __name__ == "__main__":
      sys.exit(main())
  ```

  FILE 2: pipeline/tests/test_ep_cli.py (NEW — create from scratch)

  Create this test file with the following exact structure:

  ```python
  #!/usr/bin/env python3
  """Tests for Agent CLI v1."""
  import json
  import sys
  from pathlib import Path
  from unittest.mock import MagicMock

  import pytest

  sys.path.insert(0, str(Path(__file__).parent.parent))


  @pytest.fixture(autouse=True)
  def _mock_control_plane(monkeypatch):
      """Prevent real DB/cluster connections during CLI tests."""
      mock_svc_cls = MagicMock()
      mock_svc = MagicMock()
      mock_svc.get_status_summary.return_value = {
          "active_experiments": 3,
          "completed_experiments": 1,
          "by_status": {"RUNNING": 2, "NEEDS_RERUN": 1},
          "workers_online": 1,
          "workers_total": 2,
      }
      mock_svc.list_experiments.return_value = {
          "items": [{"name": "exp1", "status": "RUNNING"}],
          "page": 1,
          "per_page": 50,
          "total": 1,
          "total_pages": 1,
      }
      mock_svc.get_experiment.return_value = {"name": "exp1", "status": "RUNNING"}
      mock_svc.get_cluster_health.return_value = {
          "cluster": {"w1": {"status": "ONLINE"}},
      }
      mock_svc_cls.return_value = mock_svc
      monkeypatch.setattr("ep_cli.ControlPlaneService", mock_svc_cls)


  def _run_cli(argv):
      from ep_cli import main
      return main(argv)


  class TestStatusCommand:
      def test_json_output(self, capsys):
          code = _run_cli(["status", "--output", "json"])
          assert code == 0
          out = capsys.readouterr().out
          data = json.loads(out)
          assert data["status"] == "ok"
          assert data["data"]["active_experiments"] == 3

      def test_text_output(self, capsys):
          code = _run_cli(["status", "--output", "text"])
          assert code == 0


  class TestExperimentsCommand:
      def test_json_output(self, capsys):
          code = _run_cli(["experiments", "--output", "json"])
          assert code == 0
          data = json.loads(capsys.readouterr().out)
          assert data["data"]["total"] == 1


  class TestExperimentDetailCommand:
      def test_found(self, capsys):
          code = _run_cli(["experiment", "exp1", "--output", "json"])
          assert code == 0
          data = json.loads(capsys.readouterr().out)
          assert data["data"]["name"] == "exp1"


  class TestClusterCommand:
      def test_json_output(self, capsys):
          code = _run_cli(["cluster", "--output", "json"])
          assert code == 0
          data = json.loads(capsys.readouterr().out)
          assert "cluster" in data["data"]


  class TestNoCommand:
      def test_returns_error(self, capsys):
          code = _run_cli([])
          assert code == 1
  ```

  Boundaries:
  - Do NOT add mutation/action subcommands. This is read-only CLI v1.
  - Do NOT import from experiments.py directly. All queries go through ControlPlaneService.
  - Do NOT touch any existing file.
  - Use cli_shared.add_common_args / emit_result / setup_logging — do not duplicate CLI framework.
acceptance_criteria:
  - "pipeline/ep_cli.py exists with main() entry point"
  - "CLI supports subcommands: status, experiments, experiment, cluster"
  - "All subcommands support --output json with stable JSON shape"
  - "pipeline/tests/test_ep_cli.py exists with tests for all subcommands"
  - "All tests pass: PYTHONPATH=pipeline python3 -m pytest pipeline/tests/test_ep_cli.py -v"
  - "Help works: PYTHONPATH=pipeline python3 -c 'from ep_cli import main; main([\"--help\"])' prints help (exits 0)"
status: completed
claimed_by: coder
claimed_at: "2026-03-26T10:07:19Z"
started_at: "2026-03-26T10:07:19Z"
completed_at: "2026-03-26T10:07:19Z"
category: architecture
effort_estimate: small
agent_type: coder
skills: []
wave: 2
qa_scenarios:
  - "PYTHONPATH=pipeline python3 -m pytest pipeline/tests/test_ep_cli.py -v => all pass"
  - "PYTHONPATH=pipeline python3 -c 'from ep_cli import main' => exit 0"
  - "python3 -m py_compile pipeline/ep_cli.py => exit 0"
source_plan: null
source_ticket_index: 2
result:
  summary: "Implemented Agent CLI v1 (`status`, `experiments`, `experiment`, `cluster`) backed by ControlPlaneService with stable JSON outputs."
  artifacts:
    - pipeline/ep_cli.py
    - pipeline/tests/test_ep_cli.py
  notes: "Read-only command surface only; no mutation commands added."
  evidence: "PYTHONPATH=pipeline python3 -m pytest pipeline/tests/test_ep_cli.py -v => 6 passed."
depends_on:
  - TKT-001
